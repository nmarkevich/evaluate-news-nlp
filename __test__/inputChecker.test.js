import { checkLink, checkText } from "../src/client/js/inputChecker.js";

/*describe ("Testing the link input from a user", () => {
    test("Testing the checkLink function", done => {
        document.body.innerHTML = 
        '<p id="LinkValidationMsg">' + '</p>' + '<input id="linkFromUser">';
        const testLink = "";
        let validationMsg;
        beforeAll(() => {
            validationMsg = jest.spyOn(document, "getElementById");
        });
         = document.getElementById("LinkValidationMsg");
        expect(checkLink(testLink).ValidationMsg.innerHTML).toBe("The link is required");
        console.log(ValidationMsg.innerHTML);
    })
});*/